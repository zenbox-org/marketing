import { Config } from '../../../../models/Config'
import { Specification } from './Specification'
import { impl } from 'zenbox-util/todo'

export async function getSpecification(project: Config): Promise<Specification> {
  throw impl()
}
