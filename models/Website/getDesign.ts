import { Config } from '../../../../models/Config'
import { Design } from './Design'
import { impl } from 'zenbox-util/todo'

export async function getDesign(project: Config): Promise<Design> {
  throw impl()
}
